# Core deep-learning stack -- install PyTorch wheel matching your platform FIRST.
# See: https://pytorch.org/get-started/locally/
#   CPU-only:        pip install torch torchvision
#   CUDA 12.x (Win): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchvision>=0.17.0

# Hugging Face
transformers>=4.40.0
huggingface_hub>=0.23.0

# Parameter-efficient fine-tuning (LoRA)
peft>=0.10.0

# Data + utils
numpy>=1.26,<3.0
pandas>=2.1.0
pillow>=10.0.0
tqdm>=4.66.0
PyYAML>=6.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
